security {

	// see DefaultSecurityConfig.groovy for all settable/overridable properties
        // useFacebook = true
        // facebook.apiKey = 'YOUR API KEY'
        // facebook.secretKey = 'YOUR SECRET KEY'
        
	active = true

	loginUserDomainClass = "Socio"
	authorityDomainClass = "Authority"
	requestMapClass = "Requestmap"
        useMail = true
        mailHost = "localhost"
        mailUsername = "antonio@localhost"
        mailPassword = "sungod"
        mailProtocol = "smtp"
        mailFrom = "user@localhost"
}
